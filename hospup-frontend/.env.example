# === BACKEND API ===
# Backend API URL (must be publicly accessible)
NEXT_PUBLIC_API_URL="https://your-backend.railway.app"

# === IMPORTANT SECURITY NOTES ===
# 1. NEVER add AWS credentials here (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
#    - Frontend code is PUBLIC and visible to users
#    - All S3 operations MUST go through backend API with presigned URLs
#
# 2. Only NEXT_PUBLIC_* variables are exposed to the browser
#    - Keep sensitive data in backend environment variables
#
# 3. This file is for LOCAL DEVELOPMENT ONLY
#    - Never commit .env.local to Git
#    - Use Vercel dashboard for production environment variables

# === OPTIONAL: ANALYTICS ===
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"  # Google Analytics
# NEXT_PUBLIC_SENTRY_DSN=""  # Sentry error tracking
